-if @valid
    .center.jumbotron
        %h1 Search Results
        %br
        %br
        %center
            %table#ride_search_results
                %thead
                    %tr
                        %td Poster ID
                        %td From
                        %td To
                        %td Year
                        %td Month
                        %td Day
                        %td Hour 
                        %td Minute
                        %td Time
                %tbody
                    -if @rides
                        -@rides.each do |ride|
                            %tr
                                -# %td= User.select(:email).find(ride[:poster_user_id])['email']
                                -# %td= "mprice3@binghamton.edu"
                                %td= ride[:poster_user_id]
                                %td= ride[:departure_location]
                                %td= ride[:destination_location]
                                %td= ride[:date_and_time].strftime("%Y")
                                %td= ride[:date_and_time].strftime("%B")
                                %td= ride[:date_and_time].strftime("%d")
                                %td= ride[:date_and_time].strftime("%I")
                                %td= ride[:date_and_time].strftime("%M")
                                %td= ride[:date_and_time].strftime("%p")
-else
    %h1= @message